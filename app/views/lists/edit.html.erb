<h1>Editing List</h1>

<%= render 'form', list: @list %>

<% @list.items.each do |li| %>
    <%= render :partial => 'items', :locals => { item: li } %>
<% end %>

<%= link_to 'Show', @list %> |
<%= link_to 'Back', lists_path %>
